{% extends 'base.html.twig' %}

{% block title %}Recettes{% endblock %}

{% block body %}
    <div class="col-md-6 contenu-blanc">
        <h1>Les <span> {{  lesRecettes | length }}  </span>catégories</h1>
        {{ include('messages.html.twig') }} 
        <div class="contenu">
            <table class="table table-striped table-advance table-hover">
                <thead>
                <tr class="bg-entete">
                    <th>Identifiant</th>
                    <th>Recette</th>
                    <th>Description</th>
                </tr>
                </thead>
                <tbody>
                {% for key, recette in lesRecettes %}
                    <tr>
                        <td>{{ recette.id }}</td>
                        <td>{{ recette.nom }}</td>
                        <td>{{ recette.nbproduits }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        
    </div>
{% endblock %}
